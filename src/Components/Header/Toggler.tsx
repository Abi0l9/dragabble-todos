import React from "react";
import { Icon } from "@iconify/react";
import moon from "@iconify/icons-mdi/star-crescent";

const Toggler = () => {
  return (
    <div>
      <Icon icon={moon} width={24} />
    </div>
  );
};

export default Toggler;
